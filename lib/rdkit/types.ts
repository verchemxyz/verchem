/**
 * RDKit TypeScript type re-exports
 *
 * Centralizes RDKit type imports so the rest of the codebase
 * does not need to depend directly on @rdkit/rdkit paths.
 */

export type { RDKitModule } from '@rdkit/rdkit'
