/**
 * Public legacy URL backed by the canonical calculator implementation.
 * Its layout declares /solutions as canonical and noindex, so this preserves
 * anonymous/no-JS access without creating a second calculation model.
 */
export { default } from '@/app/solutions/page'
